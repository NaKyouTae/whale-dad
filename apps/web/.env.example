# ─── 프론트 (apps/web) ───
# 브라우저에 노출되는 값이므로 비밀키는 절대 넣지 말 것.
# NEXT_PUBLIC_* 은 빌드 시점에 번들에 박히므로, 값을 바꾸면 반드시 다시 배포해야 한다.

# NestJS 서버 주소. 끝의 /api 까지 포함한다.
#   로컬:   http://localhost:20000/api
#   운영:   https://<render-서비스명>.onrender.com/api
NEXT_PUBLIC_API_URL=http://localhost:20000/api
