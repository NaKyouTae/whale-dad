# ─────────────────────────────────────────────
# whale-dad 공통 환경변수 예시
# 실제 값은 각 앱의 .env.local / Render / Vercel 대시보드에서 설정
# ─────────────────────────────────────────────

# 서버 (apps/server) → apps/server/.env
# 프론트 (apps/web)  → apps/web/.env.local
